
name: Airbnb CI/CD

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:

  security:
    name: Security Scan
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "npm"

      - run: npm ci
      - run: npm audit --omit=dev || true


  docker-build:
    name: Build & Push Docker Image
    runs-on: self-hosted
    needs: security

    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: |
          docker build -t ankitdockerd/airbnb:${{ github.sha }} .
          docker tag ankitdockerd/airbnb:${{ github.sha }} ankitdockerd/airbnb:latest

      - name: Push Docker Image
        run: |
          docker push ankitdockerd/airbnb:${{ github.sha }}
          docker push ankitdockerd/airbnb:latest


  deploy:
    name: Deploy to Kubernetes
    runs-on: self-hosted
    needs: docker-build

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      EKS_CLUSTER_NAME: ${{ secrets.EKS_CLUSTER_NAME }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials (If using EKS)
        if: ${{ env.AWS_ACCESS_KEY_ID != '' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update kubeconfig for EKS (Optional)
        if: ${{ env.EKS_CLUSTER_NAME != '' }}
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ env.EKS_CLUSTER_NAME }}

      - name: Update Deployment Image
        run: |
          sed -i "s|image:.*|image: ankitdockerd/airbnb:${{ github.sha }}|g" k8s/deployment.yaml
          
      - name: Apply Kubernetes Secrets
        run: kubectl apply -f k8s/secret.yaml
    

      - name: Apply Kubernetes Deployment
        run: |
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml
          kubectl apply -f k8s/ingress.yaml

      - name: Rollout Status Check
        run: |
          kubectl rollout status deployment/airbnb-app
